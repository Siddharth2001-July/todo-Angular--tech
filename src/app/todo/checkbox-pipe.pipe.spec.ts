import { CheckboxPipePipe } from './checkbox-pipe.pipe';

describe('CheckboxPipePipe', () => {
  it('create an instance', () => {
    const pipe = new CheckboxPipePipe();
    expect(pipe).toBeTruthy();
  });
});
